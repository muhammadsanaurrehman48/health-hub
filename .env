# VITE_API_URL is intentionally not set to allow dynamic network detection
# The app will auto-detect the correct API URL based on how it's accessed
# VITE_API_URL=http://localhost:5000/api
const API_URL = import.meta.env.VITE_API_URL;
